# Task Manager API

A REST API for managing tasks built with Django REST Framework.

## Tech Stack
- Python / Django
- Django REST Framework
- JWT Authentication
- django-filter

## Features
- JWT auth with token blacklisting on logout
- Custom User model with email-based login
- Full task CRUD
- ManyToMany tags on tasks
- Filter tasks by status, difficulty, and tag
- Stats endpoint
